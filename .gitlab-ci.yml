test-job1:
  image: centos:7
  stage: test
  script:
    - http_proxy=$SYSTEM_PROXY https_proxy=$SYSTEM_PROXY yum -y update && yum install -y gcc gcc-c++ python3 python3-devel make automake git libtool libtool-ltdl-devel openmpi openmpi-devel zlib-devel
    - http_proxy=$SYSTEM_PROXY https_proxy=$SYSTEM_PROXY wget https://github.com/Kitware/CMake/releases/download/v3.21.2/cmake-3.21.2-linux-x86_64.sh 
    - sh cmake-3.21.2-linux-x86_64.sh --skip-license --prefix=/usr/local && rm cmake*
    - ls -lh
